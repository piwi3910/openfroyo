---
# Agent Onboarding Inventory
# Update with your web server IPs before running

hosts:
  web-01:
    mode: ssh
    ssh_host: 192.168.1.10  # Update with your web server IP
    ssh_port: 22
    ssh_user: root
    ssh_key_file: ~/.ssh/id_rsa

  web-02:
    mode: ssh
    ssh_host: 192.168.1.11  # Update with your web server IP
    ssh_port: 22
    ssh_user: root
    ssh_key_file: ~/.ssh/id_rsa

  web-03:
    mode: ssh
    ssh_host: 192.168.1.12  # Update with your web server IP
    ssh_port: 22
    ssh_user: root
    ssh_key_file: ~/.ssh/id_rsa
