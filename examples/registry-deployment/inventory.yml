---
# Module Registry Inventory
# Update with your registry server IP before running

hosts:
  registry-server:
    mode: ssh
    ssh_host: 192.168.1.60  # Update with your registry server IP
    ssh_port: 22
    ssh_user: root
    ssh_key_file: ~/.ssh/id_rsa
