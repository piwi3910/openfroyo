hosts:
  test-host:
    ssh_host: 192.168.1.100  # Replace with your Linux host IP
    ssh_port: 22
    ssh_user: root  # Replace with your SSH user
    ssh_key_file: ~/.ssh/id_rsa  # Or use ssh_password instead
    # ssh_password: your-password  # Uncomment to use password auth
    vars:
      environment: test
