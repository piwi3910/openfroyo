name: Test Stack
inventory:
  - examples/inventory/hosts.yml

defaults:
  cmd: /usr/bin/uname -r

run:
  - name: Check kernel version
    module: generic/exec
    hosts:
      - test-host
    vars:
      cmd: /usr/bin/uname -r
