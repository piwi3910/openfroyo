name: script
version: 1.0.0
description: Copy and execute local scripts on remote hosts

steps:
  - name: Execute script
    wasm: wasm/script.wasm
    vars:
      script: "{{ var.script }}"
      args: "{{ var.args | default('') }}"
